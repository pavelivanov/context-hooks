{
  "name": "react-hooks",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.8.2",
    "react-dom": "^16.8.2",
    "react-router-dom": "^4.3.1",
    "react-scripts": "2.1.5"
  },
  "scripts": {
    "copy-lib": "rimraf ./node_modules/context-hooks && cp -r ../../lib ./node_modules/context-hooks",
    "start": "react-scripts start",
    "build": "npm run copy-lib && react-scripts build",
    "test": "npm run copy-lib && react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "devDependencies": {
    "rimraf": "^2.6.3"
  }
}
